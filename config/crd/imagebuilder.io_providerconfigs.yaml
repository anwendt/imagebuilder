---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: providerconfigs.imagebuilder.io
spec:
  group: imagebuilder.io
  names:
    kind: ProviderConfig
    listKind: ProviderConfigList
    plural: providerconfigs
    singular: providerconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.region
      name: Region
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ProviderConfig holds credentials and endpoint config for a platform provider instance.
          Intentionally namespace-scoped (no scope=Cluster): a VMImage may only reference a
          ProviderConfig in its own namespace, preventing cross-namespace credential access.
          See AS-005 (REQ-008) and SR-005 (REQ-004).
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              ProviderConfigSpec holds credentials and endpoint configuration for one
              instance of a platform provider. Multiple ProviderConfigs can exist for
              the same provider (e.g. aws-eu-west-1 and aws-us-east-1).
            properties:
              credentials:
                description: Credentials reference for the platform
                properties:
                  secretRef:
                    description: |-
                      SecretRef references a Kubernetes Secret containing the credentials.
                      The secret format is provider-specific and documented in each provider.
                    properties:
                      key:
                        description: Key is the data key within the Secret. If omitted,
                          the whole Secret is used.
                        type: string
                      name:
                        type: string
                    required:
                    - name
                    type: object
                required:
                - secretRef
                type: object
              endpoint:
                description: Endpoint for on-prem platforms (vSphere, OpenStack)
                type: string
              extra:
                additionalProperties:
                  type: string
                description: Extra holds provider-specific configuration as raw JSON
                type: object
              insecure:
                description: Insecure disables TLS verification (development only)
                type: boolean
              provider:
                description: |-
                  Provider is the name of the PlatformProvider this config belongs to
                  e.g. "aws", "vsphere", "openstack"
                type: string
              region:
                description: Region for AWS/Azure/GCP
                type: string
            required:
            - credentials
            - provider
            type: object
          status:
            properties:
              conditions:
                description: Conditions follow the standard Kubernetes condition pattern
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
